# Plan Mode Rules

## Non-Obvious Constraints
- `repo-rescue-room/` directory exists but is empty (future expansion placeholder)
- No test framework configured - would need vitest or jest
- CSS uses native nesting - requires modern browser/build tool support